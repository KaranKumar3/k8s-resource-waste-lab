apiVersion: v1
kind: Namespace
metadata:
  name: waste-lab
---
# A) Idle but heavily requested: should look "wasteful" once kubectl top is available
apiVersion: apps/v1
kind: Deployment
metadata:
  name: idle-overrequested
  namespace: waste-lab
spec:
  replicas: 3
  selector:
    matchLabels:
      app: idle-overrequested
  template:
    metadata:
      labels:
        app: idle-overrequested
    spec:
      containers:
      - name: web
        image: nginx:alpine
        resources:
          requests:
            memory: "32Gi"    # EXTREME: 2Gi → 32Gi = 30GB increase
            cpu: "4000m"      # EXTREME: 600m → 4000m = 3.4 CPU increase
            
          limits:
            memory: "1Gi"
            cpu: "1000m"
            
        ports:
        - containerPort: 80
---
# B) Requests tiny, limits huge: should look risky/over-allocated even WITHOUT metrics-server
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tiny-request-huge-limit
  namespace: waste-lab
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tiny-request-huge-limit
  template:
    metadata:
      labels:
        app: tiny-request-huge-limit
    spec:
      containers:
      - name: app
        image: hashicorp/http-echo:1.0
        args: ["-text=hello"]
        ports:
        - containerPort: 5678
        resources:
          requests:
            memory: "32Mi"
            cpu: "500m"     # Increased from 25m
          limits:
            memory: "2Gi"
            cpu: "2000m"
---
# C) No requests/limits at all: common real-world issue
apiVersion: apps/v1
kind: Deployment
metadata:
  name: no-resources-specified
  namespace: waste-lab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: no-resources-specified
  template:
    metadata:
      labels:
        app: no-resources-specified
    spec:
      containers:
      - name: app
        image: busybox
        command: ["sh","-c","while true; do echo ok; sleep 60; done"]
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
              
---
# D) Replica waste: lots of pods, no traffic generator by default
apiVersion: apps/v1
kind: Deployment
metadata:
  name: overreplicated
  namespace: waste-lab
spec:
  replicas: 8
  selector:
    matchLabels:
      app: overreplicated
  template:
    metadata:
      labels:
        app: overreplicated
    spec:
      containers:
      - name: web
        image: nginx:alpine
        resources:
          requests:
            memory: "128Mi"
            cpu: "150m"
          limits:
            memory: "256Mi"
            cpu: "300m"
            
        ports:
        - containerPort: 80
---
# E) Service bloat (unused services): your tool can detect services with no endpoints
apiVersion: v1
kind: Service
metadata:
  name: unused-service
  namespace: waste-lab
spec:
  selector:
    app: does-not-exist
  ports:
  - port: 80
    targetPort: 80
---
# F) A PVC that requests small, but PV capacity can be much larger (depending on storageclass)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: small-claim
  namespace: waste-lab
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
